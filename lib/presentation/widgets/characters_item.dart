import 'package:flutter/material.dart';

class CharactersItem extends StatelessWidget {
  const CharactersItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
